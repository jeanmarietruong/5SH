<nav class="navbar navbar-light navbar-expand-md navbar navbar-expand-lg fixed-top" id="mainNav">
    <div class="container">
        {% if app.request.get('_route') == 'show_quizz' %}
            <a class="navbar-brand js-scroll-trigger" href={{ path('home') }}>
{#                <img src="BootstrapStudio/img/logo.png" alt="">#}
            </a>
        {% else %}
            <a class="navbar-brand js-scroll-trigger" href={{ path('home') }}>
                <img src="BootstrapStudio/img/logo.png" alt="">
            </a>
        {% endif %}
        <button data-toggle="collapse" class="navbar-toggler navbar-toggler-right" data-target="#navbarResponsive" type="button" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation"
                                                                                               value="Menu">
            <i class="fa fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="nav navbar-nav ml-auto">
                {% if app.user %}
                    {#                    {{ app.user.email }}#}
                    <li class="nav-item nav-link js-scroll-trigger" role="presentation"><a class="nav-link active navButtonP js-scroll-trigger" href={{ path('profil') }}>VOTRE PROFIL</a></li>
                    <li class="nav-item nav-link js-scroll-trigger" role="presentation"><a class="nav-link navButton js-scroll-trigger" href={{ path('app_logout') }}>SE DECONNECTER</a></li>
                {% else %}
                    <li class="nav-item nav-link js-scroll-trigger" role="presentation"><a class="nav-link navButton active js-scroll-trigger" href={{ path('app_register') }}>S'INSCRIRE</a></li>
                    <li class="nav-item nav-link js-scroll-trigger " role="presentation"><a class="nav-link navButton js-scroll-trigger" href={{ path('app_login') }}>SE CONNECTER</a></li>
                {% endif %}

            </ul>
        </div>
    </div>
</nav>
